spring:
  application:
    name: goguma
  profiles:
    active: dev
  cloud:
    config:
      name: goguma
      profile: dev
  config:
    import: optional:configserver:http://localhost:8888

management:
  endpoints:
    web:
      exposure:
        include: refresh
