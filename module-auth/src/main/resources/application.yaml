spring:
  application:
    name: goguma
  profiles:
    active: dev
  config:
    import: configserver:http://localhost:8888

management:
  endpoints:
    web:
      exposure:
        include: refresh